package com.zenika.zenikia.skill.domain;

/** Broad category a {@link Skill} belongs to, used to group the candidate profile UI. */
public enum SkillCategory {
    LANGUAGE,
    FRAMEWORK_LIBRARY,
    DATA_MESSAGING,
    INFRASTRUCTURE_DEVOPS,
    ARCHITECTURE_DESIGN,
    METHODOLOGY_PROCESS,
    SOFT_SKILL,
    OTHER
}
