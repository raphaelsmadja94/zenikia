/**
 * Backend base URL. Hardcoded for the POC (single local backend on
 * :8080) — see README "How to run" if you need to point at a different
 * host.
 */
export const API_BASE_URL = 'http://localhost:8080/api';
